import { DocumentSnapshot, getDocs, collection } from 'firebase/firestore';

// Utility functions for extracting user metadata from Firebase
export const extractUserMeta = {
  // Add utility functions here as needed
};

// Default export to satisfy routing requirements
export default extractUserMeta;